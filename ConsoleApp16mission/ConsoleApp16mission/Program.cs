using System;

namespace ConsoleApp16mission
{
    class Program
    {
        static void Main(string[] args)
        {
            Console.WriteLine("Hello World!");

            //namespace:Member
            //memberVO(num, id, pw, name, tel),
            //MemberDAO:class(insert, update, delete, selectOne, selectAll, searchList)

            //namespace:Board
            //BoardVO(num, title, content, writer, wdate),
            //BoardDAO:class(insert, update, delete, selectOne, selectAll, searchList)

            //클래스 생성, g/s
            //main : 객체 생성 및 메소드 호출 확인 
        }
    }
}
